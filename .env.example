# CDIL Environment Variables - Example Configuration
# 
# Copy this file to .env and fill in your values
# DO NOT commit .env to git (it's in .gitignore)

# ============================================
# JWT Configuration (REQUIRED)
# ============================================
# Generate a strong secret: openssl rand -base64 32
# NEVER use the dev key in production
JWT_SECRET_KEY=your-secret-key-here
JWT_ALGORITHM=HS256

# ============================================
# Database Configuration
# ============================================
CDIL_DB_PATH=/data/eli_sentinel.db
# Full SQLAlchemy URL for PostgreSQL (overrides CDIL_DB_PATH)
# DATABASE_URL=postgresql+psycopg2://cdil:password@localhost:5432/cdil

# ============================================
# Logging Configuration
# ============================================
LOG_LEVEL=INFO
LOG_FORMAT=json

# ============================================
# Rate Limiting
# ============================================
RATE_LIMIT_ENABLED=true
# Set to 1 to disable rate limiting (for tests/dev only, never in production)
DISABLE_RATE_LIMITS=0

# ============================================
# PHI Safety
# ============================================
# Default: false — note text is NEVER stored in production
# Set to true only in explicit research/audit environments with appropriate controls
STORE_NOTE_TEXT=false

# ============================================
# Runtime Environment
# ============================================
# Set to TEST to disable rate limiting in automated tests
# ENV=TEST

# ============================================
# Uvicorn Workers (optional)
# ============================================
# Recommended: 2x CPU cores
# UVICORN_WORKERS=4
